# deps
node_modules/
.pnp.*
.yarn/*

# build
dist/
build/
.next/
out/
*.tsbuildinfo

# env
.env
.env.*
!.env.example

# python
.venv/
__pycache__/
*.py[cod]

# os / editor
.DS_Store
.idea/
*.log

# claude local
.claude/settings.local.json

# secrets
*.pem

# map assets (see SOURCE.md + scripts/) — never commit the source image or tiles
data/map/
data/*.tmp
*.tsbuildinfo

# th.gl node dump in canonical px (docs/DECISIONS.md D14) — local only, never commit
data/pois.json
